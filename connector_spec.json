{
  "version": "2.0.0",
  "fields": [
    {
      "field": "AUTH_TYPE",
      "displayName": "Genai-Test-002 Auth Type",
      "type": "String",
      "minLength": 4,
      "maxLength": 60,
      "required": true,
      "description": "Genai-Test-002 API Auth Type",
      "default": "NONE",
      "example": "NONE",
      "helpSection": ""
    },
    {
      "field": "SERVER_URL",
      "displayName": "Genai-Test-002 Server Url",
      "type": "String",
      "minLength": 3,
      "maxLength": 40,
      "required": true,
      "description": "Genai-Test-002 API Server Url",
      "default": "",
      "example": "http://",
      "helpSection": "1. Navigate to the developer portal. 2. Expand the Projects and Apps dropdown in the side navigation. 3. Open the app that is associated with the API key and secret that you want to find or regenerate. 4. Navigate to the Keys and tokens tab. From there, you will find all of the credentials associated with your app. "
    }
  ]
}